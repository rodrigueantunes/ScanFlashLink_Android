<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/backgroundColor"
    tools:context=".MainActivity">

    <!-- Bloc contenant l'entrée IP et le bouton -->
    <LinearLayout
        android:id="@+id/ipContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.4">

        <!-- Champ de saisie d'IP modernisé -->
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            app:boxBackgroundMode="outline"
            app:boxCornerRadiusTopStart="12dp"
            app:boxCornerRadiusTopEnd="12dp"
            app:boxCornerRadiusBottomStart="12dp"
            app:boxCornerRadiusBottomEnd="12dp"
            app:hintTextColor="@color/hintColor"
            app:strokeColor="@color/cardBorderColor">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/edtServerIp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Entrez l'IP du serveur"
                android:inputType="text"
                android:padding="14dp"
                android:textColor="@color/textColor"
                android:textColorHint="@color/hintColor"/>
        </com.google.android.material.textfield.TextInputLayout>

        <!-- Bouton moderne avec Material Design -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSaveIp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/buttonColor"
            android:paddingVertical="12dp"
            android:text="Enregistrer l'IP"
            android:textAllCaps="false"
            android:textColor="@color/white"
            app:cornerRadius="14dp"
            app:rippleColor="@color/rippleEffect"/>
    </LinearLayout>

    <!-- Zone de prévisualisation de la caméra (taille réduite) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/materialCardView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:backgroundTint="@color/cardBackground"
        app:cardCornerRadius="14dp"
        app:cardElevation="6dp"
        app:strokeColor="@color/cardBorderColor"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toBottomOf="@id/ipContainer"
        app:layout_constraintBottom_toTopOf="@id/txtScanResult"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.4"> <!-- 📌 Taille réduite à 40% -->

        <android.view.TextureView
            android:id="@+id/cameraPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </com.google.android.material.card.MaterialCardView>

    <!-- Résultat du scan en bas, aligné avec edtServerIp et btnSaveIp -->
    <TextView
        android:id="@+id/txtScanResult"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="14dp"
        android:text="Scanne un code-barres"
        android:textColor="@color/textColor"
        android:textSize="18sp"
        android:gravity="center"
        android:background="@drawable/text_view_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/ipContainer"
        app:layout_constraintEnd_toEndOf="@id/ipContainer"/>
</androidx.constraintlayout.widget.ConstraintLayout>
